seccion: PruebaTotal {

    // Ejemplo codigo intermedio
    definir x = 4;
    definir y = 5;
    definir z = x * y + 2; 

    mostrar("Iniciando prueba maestra del compilador");
    definir usuario = leer("Presiona Enter para continuar...");

    definir n1 = nota do octava 4;
    definir n2 = nota mi octava 5;
    definir n3 = nota sol octava 4;

    acorde MiAcorde (n1, n2, n3);
    
    melodia MiMelodia { n1, n2, nota la octava 4 };

    // --- 5. PRUEBA DE CONTROL DE FLUJO ---
    // El C3D generará operadores relacionales.
    si_ (z > 20) {
        
        mostrar("Las matematicas y el semantico funcionan perfecto.");
        tocar(MiAcorde);
        
        repetir x veces {
            tocar(MiMelodia);
            silencio;
        }
        
    } sino {
        romper;
    }

} fin