seccion: PruebaFinal {

    // --- 1. PRUEBA DE OPTIMIZACIÓN: Plegamiento de Constantes ---
    // El C3D original hará la multiplicación y suma. 
    // El C3D Optimizado lo convertirá directo en: x = 50, y = 52
    definir x = 10 * 5;
    definir y = x + 2;

    // --- 2. PRUEBA DE OPTIMIZACIÓN: Simplificación Algebraica ---
    // El C3D Optimizado eliminará los ceros y unos innecesarios.
    definir redundante = y * 1;
    definir tonto = y + 0;
    definir anulado = y * 0;

    // --- 3. PRUEBA DE SEMÁNTICA MUSICAL ESTRICTA ---
    // Octavas válidas (0-8) y asignación correcta de tipos
    definir n1 = nota do octava 4;
    definir n2 = nota mi octava 5;
    definir n3 = nota sol;

    // --- 4. PRUEBA DE ACORDES Y MELODÍAS ---
    // Acorde exige mínimo 2 notas separadas por coma
    acorde AcordeFinal (n1, n2, n3);
    
    // Melodía solo acepta notas
    melodia MelodiaFinal { n1, n2, nota la octava 4 };

    // --- 5. PRUEBA DE INTERFAZ Y CONTROL DE FLUJO ---
    mostrar("Generando Cuadruplos Optimizados...");
    definir usuario = leer("Presiona Enter para escuchar:");

    si_ (x > 40) {
        
        repetir 2 veces {
            tocar(MelodiaFinal);
            silencio;
        }
        
        tocar(AcordeFinal);
        mostrar("¡Compilador NotaScript terminado con exito!");
        
    } sino {
        romper;
    }

} fin